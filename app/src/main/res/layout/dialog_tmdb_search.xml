<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <!-- Champ de recherche avec croix d'effacement intégrée -->
        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp">

            <EditText
                android:id="@+id/et_tmdb_query"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_input_search"
                android:hint="@string/hint_search_tmdb"
                android:inputType="text"
                android:imeOptions="flagNoExtractUi|actionSearch"
                android:paddingStart="12dp"
                android:paddingEnd="46dp"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"
                android:textSize="14sp"
                android:singleLine="true" />

            <!-- Croix : efface le texte, focusable à la télécommande -->
            <ImageView
                android:id="@+id/btn_clear_tmdb"
                android:layout_width="34dp"
                android:layout_height="34dp"
                android:layout_gravity="end|center_vertical"
                android:layout_marginEnd="5dp"
                android:padding="7dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_clear"
                android:background="@drawable/bg_clear_btn"
                android:focusable="true"
                android:contentDescription="@string/btn_clear_search" />

            <com.nicotv.iptv.ui.common.RotatingBorderView
                android:id="@+id/search_ring"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="invisible" />
        </FrameLayout>

        <Button
            android:id="@+id/btn_tmdb_search"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:background="@drawable/bg_btn_primary"
            android:text="@string/btn_search"
            android:textColor="@android:color/white"
            android:textSize="13sp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:focusable="true" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/progress_tmdb"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="4dp"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_tmdb_results"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:clipToPadding="false" />

</LinearLayout>
